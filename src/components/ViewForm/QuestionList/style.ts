import { styled } from 'styled-components'

const Main = styled.div`
  position: relative;
  width: 100%;
  justify-content: center;
`

const QuestionList = styled.div`
  display: flex;
  flex-direction: column;
  gap: 12px;
  width: 100%;
  align-items: center;
  position: relative;
`

export { Main, QuestionList }
